Download zoom by debian package and install in ubuntu

wget -O Downloads/zoom.deb https://zoom.us/client/latest/zoom_amd64.deb
cd Downloads
ls
sudo dpkg -i zoom.deb
sudo apt-get install -f
sudo dpkg -i zoom.deb
